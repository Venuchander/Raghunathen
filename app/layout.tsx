import './globals.css'
import type { Metadata } from 'next'
import { Inter } from 'next/font/google'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Raghunathen - Full Stack Developer & UI/UX Enthusiast',
  description: 'Portfolio of Raghunathen, a Full Stack Developer with over 5 years of experience in crafting robust web applications.',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}

